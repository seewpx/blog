// 站点设置。放在 GitHub Pages 上时这个文件可以原样不动 —— 仓库名会从
// 页面地址里推出来(见 assets/site.js 的 repoFor):
//   https://<用户名>.github.io/<仓库名>/...  →  <用户名>/<仓库名>
//   https://<用户名>.github.io/...           →  <用户名>/<用户名>.github.io
const SITE = {
  // 站点名(页脚显示;页眉里的名字在 HTML 里,想改就全局替换)
  title: "单机辅助手记",

  // 只有下面两种情况才需要填(否则留空即可):
  //   1) 你用了自定义域名 —— 页面地址里看不出仓库名;
  //   2) 站点仓库和放 Release 的仓库不是同一个。
  // 格式 "用户名/仓库名"。填了它就以它为准,不再从地址推断。
  repo: "",
};
